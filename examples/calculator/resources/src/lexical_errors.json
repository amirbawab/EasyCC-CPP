{
    "default_message": "Error found '${value}' at line ${line} and column ${column}",
    "error_messages": {
        "T_INVALID_CHAR" : "Invalid character '${value}' found at line ${line} and column ${column}"
    }
}
